{
  "name": "fastify-ts-crud",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
